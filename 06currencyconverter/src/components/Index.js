// it is a file made so that we cann collect all components here and then export them to app.jsx in order to avoid multiple export statements in main code file
import InputBox from "./InputBox";
export {InputBox};